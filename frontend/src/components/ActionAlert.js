import * as React from 'react';
import {Button, } from '@material-ui/core';
import Alert from '@material-ui/lab/Alert';

export default function ActionAlerts() {
  return (
<Alert onClose={() => {}}>Created Successfully!</Alert>
  );
}